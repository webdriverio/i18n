---
id: gettingstarted
title: Початок роботи
---

Ласкаво просимо до документації WebdriverIO. Вона допоможе вам швидко почати роботу. Якщо у вас виникнуть проблеми, ви можете знайти допомогу та відповіді на нашому [Discord Сервері Підтримки](https://discord.webdriver.io) або зв'язатися з нами через [𝕏](https://x.com/webdriverio).

:::info
Це документація для найновішої версії (__>=9.x__) WebdriverIO. Якщо ви все ще використовуєте старішу версію, будь ласка, відвідайте [старі веб-сайти документації](/versions)!
:::

<LiteYouTubeEmbed
    id="rA4IFNyW54c"
    title="Getting Started with WebdriverIO"
/>

:::tip Офіційний YouTube канал 🎥

Ви можете знайти більше відео про WebdriverIO на [офіційному YouTube каналі](https://youtube.com/@webdriverio). Не забудьте підписатися!

:::

## Ініціалізація налаштування WebdriverIO

Щоб додати повне налаштування WebdriverIO до існуючого чи нового проекту, використовуючи [WebdriverIO Starter Toolkit](https://www.npmjs.com/package/create-wdio), виконайте:

Якщо ви знаходитесь у кореневому каталозі існуючого проекту, запустіть:

<Tabs
  defaultValue="npm"
  values={[
    {label: 'NPM', value: 'npm'},
    {label: 'Yarn', value: 'yarn'},
    {label: 'pnpm', value: 'pnpm'},
    {label: 'bun', value: 'bun'},
  ]
}>
<TabItem value="npm">

```sh
npm init wdio@latest .
```

або якщо ви хочете створити новий проект:

```sh
npm init wdio@latest ./path/to/new/project
```

</TabItem>
<TabItem value="yarn">

```sh
yarn create wdio .
```

або якщо ви хочете створити новий проект:

```sh
yarn create wdio ./path/to/new/project
```

</TabItem>
<TabItem value="pnpm">

```sh
pnpm create wdio@latest .
```

або якщо ви хочете створити новий проект:

```sh
pnpm create wdio@latest ./path/to/new/project
```

</TabItem>
<TabItem value="bun">

```sh
bun create wdio@latest .
```

або якщо ви хочете створити новий проект:

```sh
bun create wdio@latest ./path/to/new/project
```

</TabItem>
</Tabs>

Ця єдина команда завантажує інструмент CLI WebdriverIO та запускає майстер налаштування, який допомагає вам налаштувати ваш набір тестів.

<CreateProjectAnimation />

Майстер запропонує набір запитань, які проведуть вас через налаштування. Ви можете передати параметр `--yes`, щоб вибрати налаштування за замовчуванням, яке використовуватиме Mocha з Chrome за допомогою шаблону [Page Object](https://martinfowler.com/bliki/PageObject.html).

<Tabs
  defaultValue="npm"
  values={[
    {label: 'NPM', value: 'npm'},
    {label: 'Yarn', value: 'yarn'},
    {label: 'pnpm', value: 'pnpm'},
    {label: 'bun', value: 'bun'},
  ]
}>
<TabItem value="npm">

```sh
npm init wdio@latest . -- --yes
```

</TabItem>
<TabItem value="yarn">

```sh
yarn create wdio . --yes
```

</TabItem>
<TabItem value="pnpm">

```sh
pnpm create wdio@latest . --yes
```

</TabItem>
<TabItem value="bun">

```sh
bun create wdio@latest . --yes
```

</TabItem>
</Tabs>

## Встановити CLI вручну

Ви також можете додати пакет CLI до свого проекту вручну через:

```sh
npm i --save-dev @wdio/cli
npx wdio --version # виводить, наприклад, `8.13.10`

# запустити майстер налаштування
npx wdio config
```

## Запуск тесту

Ви можете запустити свій набір тестів, використовуючи команду `run` та вказавши на конфігурацію WebdriverIO, яку ви щойно створили:

```sh
npx wdio run ./wdio.conf.js
```

Якщо ви хочете запустити конкретні тестові файли, ви можете додати параметр `--spec`:

```sh
npx wdio run ./wdio.conf.js --spec example.e2e.js
```

або визначити набори у вашому файлі конфігурації та запустити лише тестові файли, визначені в наборі:

```sh
npx wdio run ./wdio.conf.js --suite exampleSuiteName
```

## Запуск у скрипті

Якщо ви хочете використовувати WebdriverIO як двигун автоматизації в [Автономному режимі](/docs/setuptypes#standalone-mode) у скрипті Node.JS, ви також можете безпосередньо встановити WebdriverIO та використовувати його як пакет, наприклад, для створення знімка екрана веб-сайту:

```js reference useHTTPS
https://github.com/webdriverio/example-recipes/blob/fc362f2f8dd823d294b9bb5f92bd5991339d4591/getting-started/run-in-script.js#L2-L19
```

__Примітка:__ всі команди WebdriverIO асинхронні і повинні бути правильно оброблені за допомогою [`async/await`](https://javascript.info/async-await).

## Запис тестів

WebdriverIO надає інструменти, які допоможуть вам розпочати роботу, записуючи ваші тестові дії на екрані та автоматично генеруючи тестові скрипти WebdriverIO. Див. [Запис тестів за допомогою Chrome DevTools Recorder](/docs/record) для отримання додаткової інформації.

## Системні вимоги

Вам знадобиться встановлений [Node.js](http://nodejs.org).

- Встановіть принаймні v18.20.0 або вище, оскільки це найстаріша активна версія LTS
- Офіційно підтримуються лише релізи, які є або стануть релізами LTS

Якщо Node ще не встановлений у вашій системі, ми пропонуємо використовувати такі інструменти, як [NVM](https://github.com/creationix/nvm) або [Volta](https://volta.sh/) для допомоги в управлінні кількома активними версіями Node.js. NVM є популярним вибором, а Volta також є хорошою альтернативою.