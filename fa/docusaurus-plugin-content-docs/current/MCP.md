---
id: mcp
title: MCP (پروتکل بافتار مدل)
---

## چه کاری می‌تواند انجام دهد؟

WebdriverIO MCP یک **سرور پروتکل بافتار مدل (MCP)** است که به دستیاران هوش مصنوعی مانند Claude Desktop و Claude Code امکان می‌دهد تا مرورگرهای وب و برنامه‌های موبایل را خودکارسازی کرده و با آنها تعامل کنند.

### چرا WebdriverIO MCP؟

-   **اولویت با موبایل**: برخلاف سرورهای MCP که فقط مرورگر را پشتیبانی می‌کنند، WebdriverIO MCP از خودکارسازی برنامه‌های بومی iOS و Android از طریق Appium پشتیبانی می‌کند
-   **انتخابگرهای چندسکویی**: تشخیص هوشمند عناصر به طور خودکار چندین استراتژی مکان‌یابی (شناسه دسترسی‌پذیری، XPath، UiAutomator، محمول‌های iOS) تولید می‌کند
-   **اکوسیستم WebdriverIO**: بر روی چارچوب آزمایش‌شده WebdriverIO با اکوسیستم غنی از سرویس‌ها و گزارشگرهای آن ساخته شده است

این سرور یک رابط یکپارچه برای موارد زیر فراهم می‌کند:

-   🖥️ **مرورگرهای دسکتاپ** (Chrome - در حالت سربرگی یا بدون سربرگ)
-   📱 **برنامه‌های بومی موبایل** (شبیه‌سازهای iOS / شبیه‌سازهای Android / دستگاه‌های واقعی از طریق Appium)
-   📳 **برنامه‌های هیبریدی موبایل** (تعویض بافتار بومی + WebView از طریق Appium)

از طریق بسته [`@wdio/mcp`](https://www.npmjs.com/package/@wdio/mcp).

این به دستیاران هوش مصنوعی امکان می‌دهد:

-   **راه‌اندازی و کنترل مرورگرها** با ابعاد قابل تنظیم، حالت بدون سربرگ و ناوبری اولیه اختیاری
-   **پیمایش وب‌سایت‌ها** و تعامل با عناصر (کلیک، تایپ، اسکرول)
-   **تحلیل محتوای صفحه** از طریق درخت دسترسی‌پذیری و تشخیص عناصر قابل مشاهده با پشتیبانی از صفحه‌بندی
-   **گرفتن اسکرین‌شات** به طور خودکار بهینه‌سازی شده (تغییر اندازه، فشرده‌سازی به حداکثر ۱ مگابایت)
-   **مدیریت کوکی‌ها** برای مدیریت نشست
-   **کنترل دستگاه‌های موبایل** شامل حرکات (ضربه زدن، کشیدن، کشیدن و رها کردن)
-   **تعویض بافتارها** در برنامه‌های هیبریدی بین حالت بومی و webview
-   **اجرای اسکریپت‌ها** - جاوااسکریپت در مرورگرها، دستورات موبایل Appium روی دستگاه‌ها
-   **مدیریت ویژگی‌های دستگاه** مانند چرخش، صفحه کلید، موقعیت جغرافیایی
-   و موارد بسیار دیگر، به [ابزارها](./mcp/tools) و [پیکربندی](./mcp/configuration) مراجعه کنید

:::info

توجه برای برنامه‌های موبایل
خودکارسازی موبایل به یک سرور Appium در حال اجرا با درایورهای مناسب نصب شده نیاز دارد. برای دستورالعمل‌های راه‌اندازی به [پیش‌نیازها](#prerequisites) مراجعه کنید.

:::

## نصب

ساده‌ترین راه استفاده از `@wdio/mcp` از طریق npx بدون هیچ نصب محلی است:

```sh
npx @wdio/mcp
```

یا آن را به صورت سراسری نصب کنید:

```sh
npm install -g @wdio/mcp
```

## استفاده با Claude

برای استفاده از WebdriverIO MCP با Claude، فایل پیکربندی را اصلاح کنید:

```json
{
    "mcpServers": {
        "wdio-mcp": {
            "command": "npx",
            "args": ["-y", "@wdio/mcp"]
        }
    }
}
```

پس از افزودن پیکربندی، Claude را مجدداً راه‌اندازی کنید. ابزارهای WebdriverIO MCP برای وظایف خودکارسازی مرورگر و موبایل در دسترس خواهند بود.

### استفاده با Claude Code

Claude Code به طور خودکار سرورهای MCP را تشخیص می‌دهد. می‌توانید آن را در فایل `.claude/settings.json` یا `.mcp.json` پروژه خود پیکربندی کنید.

یا با اجرای دستور زیر آن را به صورت سراسری به .claude.json اضافه کنید:
```bash
claude mcp add --transport stdio wdio-mcp -- npx -y @wdio/mcp
```
با اجرای دستور `/mcp` در داخل claude code آن را تأیید کنید.

## مثال‌های شروع سریع

### خودکارسازی مرورگر

از Claude بخواهید وظایف مرورگر را خودکار کند:

```
"Chrome را باز کن و به https://webdriver.io برو"
"روی دکمه 'Get Started' کلیک کن"
"از صفحه اسکرین‌شات بگیر"
"تمام لینک‌های قابل مشاهده در صفحه را پیدا کن"
```

### خودکارسازی برنامه موبایل

از Claude بخواهید برنامه‌های موبایل را خودکار کند:

```
"برنامه iOS من را در شبیه‌ساز آیفون ۱۵ شروع کن"
"روی دکمه ورود ضربه بزن"
"برای اسکرول به پایین، به بالا بکش"
"از صفحه فعلی اسکرین‌شات بگیر"
```

## قابلیت‌ها

### خودکارسازی مرورگر (Chrome)

| ویژگی | توضیحات |
|---------|-------------|
| **مدیریت نشست** | راه‌اندازی Chrome در حالت سربرگی/بدون سربرگ با ابعاد سفارشی و URL ناوبری اختیاری |
| **ناوبری** | پیمایش به URL‌ها |
| **تعامل با عناصر** | کلیک روی عناصر، تایپ متن، یافتن عناصر با انتخابگرهای مختلف |
| **تحلیل صفحه** | دریافت عناصر قابل مشاهده (با صفحه‌بندی)، درخت دسترسی‌پذیری (با فیلترینگ) |
| **اسکرین‌شات** | ثبت اسکرین‌شات (بهینه‌سازی خودکار تا حداکثر ۱ مگابایت) |
| **اسکرول** | اسکرول به بالا/پایین با مقادیر پیکسل قابل تنظیم |
| **مدیریت کوکی** | دریافت، تنظیم و حذف کوکی‌ها |
| **اجرای اسکریپت** | اجرای جاوااسکریپت سفارشی در بافتار مرورگر |

### خودکارسازی برنامه موبایل (iOS/Android)

| ویژگی | توضیحات |
|---------|-------------|
| **مدیریت نشست** | راه‌اندازی برنامه‌ها در شبیه‌سازها، شبیه‌سازهای نرم‌افزاری یا دستگاه‌های واقعی |
| **حرکات لمسی** | ضربه زدن، کشیدن، کشیدن و رها کردن |
| **تشخیص عنصر** | تشخیص هوشمند عناصر با چندین استراتژی مکان‌یابی و صفحه‌بندی |
| **چرخه حیات برنامه** | بررسی وضعیت برنامه (از طریق `execute_script` برای فعال‌سازی/خاتمه) |
| **تعویض بافتار** | تعویض بین بافتارهای بومی و webview در برنامه‌های هیبریدی |
| **کنترل دستگاه** | چرخش دستگاه، کنترل صفحه کلید |
| **موقعیت جغرافیایی** | دریافت و تنظیم مختصات GPS دستگاه |
| **مجوزها** | مدیریت خودکار مجوزها و هشدارها |
| **اجرای اسکریپت** | اجرای دستورات موبایلی Appium (pressKey، deepLink، shell و غیره) |

## پیش‌نیازها

### خودکارسازی مرورگر

-   **Chrome** باید روی سیستم شما نصب شده باشد
-   WebdriverIO مدیریت خودکار ChromeDriver را انجام می‌دهد

### خودکارسازی موبایل

#### iOS

1. **Xcode را از App Store مک نصب کنید**
2. **ابزارهای خط فرمان Xcode را نصب کنید**:
   ```sh
   xcode-select --install
   ```
3. **Appium را نصب کنید**:
   ```sh
   npm install -g appium
   ```
4. **درایور XCUITest را نصب کنید**:
   ```sh
   appium driver install xcuitest
   ```
5. **سرور Appium را شروع کنید**:
   ```sh
   appium
   ```
6. **برای شبیه‌سازها**: Xcode → Window → Devices and Simulators را باز کنید تا شبیه‌سازها را ایجاد/مدیریت کنید
7. **برای دستگاه‌های واقعی**: به UDID دستگاه نیاز دارید (شناسه منحصر به فرد ۴۰ کاراکتری)

#### Android

1. **Android Studio را نصب کنید** و Android SDK را راه‌اندازی کنید
2. **متغیرهای محیطی را تنظیم کنید**:
   ```sh
   export ANDROID_HOME=$HOME/Library/Android/sdk
   export PATH=$PATH:$ANDROID_HOME/emulator
   export PATH=$PATH:$ANDROID_HOME/platform-tools
   ```
3. **Appium را نصب کنید**:
   ```sh
   npm install -g appium
   ```
4. **درایور UiAutomator2 را نصب کنید**:
   ```sh
   appium driver install uiautomator2
   ```
5. **سرور Appium را شروع کنید**:
   ```sh
   appium
   ```
6. **یک شبیه‌ساز ایجاد کنید** از طریق Android Studio → Virtual Device Manager
7. **شبیه‌ساز را قبل از اجرای تست‌ها شروع کنید**

## معماری

### نحوه کارکرد

WebdriverIO MCP به عنوان پلی بین دستیاران هوش مصنوعی و خودکارسازی مرورگر/موبایل عمل می‌کند:

```
┌─────────────────┐     MCP Protocol      ┌─────────────────┐
│  Claude Desktop │ ◄──────────────────►  │    @wdio/mcp    │
│  or Claude Code │      (stdio)          │     Server      │
└─────────────────┘                       └────────┬────────┘
                                                   │
                                             WebDriverIO API
                                                   │
                    ┌──────────────────────────────┼──────────────────────────────┐
                    │                              │                              │
            ┌───────▼───────┐             ┌───────▼───────┐             ┌───────▼───────┐
            │    Chrome     │             │    Appium     │             │    Appium     │
            │   (Browser)   │             │     (iOS)     │             │   (Android)   │
            └───────────────┘             └───────────────┘             └───────────────┘
```

### مدیریت نشست

-   **مدل تک‌نشستی**: در هر زمان فقط یک نشست مرورگر یا برنامه می‌تواند فعال باشد
-   **وضعیت نشست** به صورت سراسری در تمامی فراخوانی‌های ابزار حفظ می‌شود
-   **جداسازی خودکار**: نشست‌های با وضعیت حفظ شده (`noReset: true`) به طور خودکار در هنگام بستن جدا می‌شوند

### تشخیص عنصر

#### مرورگر (وب)

-   از یک اسکریپت بهینه‌سازی شده مرورگر برای یافتن تمام عناصر قابل مشاهده و قابل تعامل استفاده می‌کند
-   عناصر را با انتخابگرهای CSS، شناسه‌ها، کلاس‌ها و اطلاعات ARIA برمی‌گرداند
-   به طور پیش‌فرض عناصر قابل مشاهده در ویوپورت را فیلتر می‌کند

#### موبایل (برنامه‌های بومی)

-   از تجزیه کارآمد منبع صفحه XML استفاده می‌کند (۲ فراخوانی HTTP در مقابل ۶۰۰+ برای پرس‌وجوهای سنتی)
-   طبقه‌بندی عناصر مختص پلتفرم برای Android و iOS
-   چندین استراتژی مکان‌یابی برای هر عنصر تولید می‌کند:
    -   شناسه دسترسی‌پذیری (چندسکویی، پایدارترین)
    -   شناسه منبع / ویژگی نام
    -   تطبیق متن / برچسب
    -   XPath (کامل و ساده‌شده)
    -   UiAutomator (Android) / Predicates (iOS)

## نحو انتخابگر

سرور MCP از چندین استراتژی انتخابگر پشتیبانی می‌کند. برای مستندات دقیق به [انتخابگرها](./mcp/selectors) مراجعه کنید.

### وب (CSS/XPath)

```
# انتخابگرهای CSS
button.my-class
#element-id
[data-testid="login"]

# XPath
//button[@class='submit']
//a[contains(text(), 'Click')]

# انتخابگرهای متنی (مختص WebdriverIO)
button=Exact Button Text
a*=Partial Link Text
```

### موبایل (چندسکویی)

```
# شناسه دسترسی‌پذیری (توصیه شده - در iOS و Android کار می‌کند)
~loginButton

# Android UiAutomator
android=new UiSelector().text("Login")

# رشته محمول iOS
-ios predicate string:label == "Login"

# زنجیره کلاس iOS
-ios class chain:**/XCUIElementTypeButton[`label == "Login"`]

# XPath (در هر دو پلتفرم کار می‌کند)
//android.widget.Button[@text="Login"]
//XCUIElementTypeButton[@label="Login"]
```

## ابزارهای موجود

سرور MCP ۲۵ ابزار برای خودکارسازی مرورگر و موبایل ارائه می‌دهد. برای مرجع کامل به [ابزارها](./mcp/tools) مراجعه کنید.

### ابزارهای مرورگر

| ابزار | توضیحات |
|------|-------------|
| `start_browser` | راه‌اندازی مرورگر Chrome (با URL اولیه اختیاری) |
| `close_session` | بستن یا جداسازی از نشست |
| `navigate` | پیمایش به یک URL |
| `click_element` | کلیک روی یک عنصر |
| `set_value` | تایپ متن در ورودی |
| `get_visible_elements` | دریافت عناصر قابل مشاهده/قابل تعامل (با صفحه‌بندی) |
| `get_accessibility` | دریافت درخت دسترسی‌پذیری (با فیلترینگ) |
| `take_screenshot` | ثبت اسکرین‌شات (بهینه‌سازی خودکار) |
| `scroll` | اسکرول صفحه به بالا یا پایین |
| `get_cookies` / `set_cookie` / `delete_cookies` | مدیریت کوکی‌ها |
| `execute_script` | اجرای جاوااسکریپت در بافتار مرورگر |

### ابزارهای موبایل

| ابزار | توضیحات |
|------|-------------|
| `start_app_session` | راه‌اندازی برنامه iOS/Android |
| `tap_element` | ضربه زدن روی عنصر یا مختصات |
| `swipe` | کشیدن در یک جهت |
| `drag_and_drop` | کشیدن بین مکان‌ها |
| `get_app_state` | بررسی اینکه آیا برنامه در حال اجرا است |
| `get_contexts` / `switch_context` | تعویض بافتار برنامه هیبریدی |
| `rotate_device` | چرخش به حالت عمودی/افقی |
| `get_geolocation` / `set_geolocation` | دریافت یا تنظیم مختصات GPS |
| `hide_keyboard` | بستن صفحه کلید روی صفحه |
| `execute_script` | اجرای دستورات موبایل Appium |

## مدیریت خودکار

### مجوزها

به طور پیش‌فرض، سرور MCP به طور خودکار مجوزهای برنامه را اعطا می‌کند (`autoGrantPermissions: true`)، که نیاز به مدیریت دستی دیالوگ‌های مجوز در طول خودکارسازی را برطرف می‌کند.

### هشدارهای سیستم

هشدارهای سیستم (مانند "اجازه اعلان‌ها؟") به طور پیش‌فرض به طور خودکار پذیرفته می‌شوند (`autoAcceptAlerts: true`). این می‌تواند برای رد کردن به جای پذیرش با `autoDismissAlerts: true` پیکربندی شود.

## پیکربندی

### متغیرهای محیطی

اتصال سرور Appium را پیکربندی کنید:

| متغیر | پیش‌فرض | توضیحات |
|----------|---------|-------------|
| `APPIUM_URL` | `127.0.0.1` | نام میزبان سرور Appium |
| `APPIUM_URL_PORT` | `4723` | پورت سرور Appium |
| `APPIUM_PATH` | `/` | مسیر سرور Appium |

### مثال با سرور Appium سفارشی

```json
{
    "mcpServers": {
        "wdio-mcp": {
            "command": "npx",
            "args": ["-y", "@wdio/mcp"],
            "env": {
                "APPIUM_URL": "192.168.1.100",
                "APPIUM_URL_PORT": "4724"
            }
        }
    }
}
```

## بهینه‌سازی عملکرد

سرور MCP برای ارتباط کارآمد دستیار هوش مصنوعی بهینه‌سازی شده است:

-   **فرمت TOON**: از نشانه‌گذاری شیء جهت‌دار توکن برای استفاده حداقلی از توکن استفاده می‌کند
-   **تجزیه XML**: تشخیص عنصر موبایل از ۲ فراخوانی HTTP استفاده می‌کند (در مقابل ۶۰۰+ به روش سنتی)
-   **فشرده‌سازی اسکرین‌شات**: تصاویر به طور خودکار به حداکثر ۱ مگابایت با استفاده از Sharp فشرده می‌شوند
-   **فیلترینگ ویوپورت**: به طور پیش‌فرض فقط عناصر قابل مشاهده برگردانده می‌شوند
-   **صفحه‌بندی**: لیست‌های بزرگ عناصر می‌توانند صفحه‌بندی شوند تا اندازه پاسخ کاهش یابد

## پشتیبانی از TypeScript

سرور MCP در TypeScript نوشته شده است و شامل تعاریف کامل نوع است. اگر در حال گسترش یا ادغام با سرور به صورت برنامه‌نویسی هستید، از تکمیل خودکار و ایمنی نوع بهره‌مند خواهید شد.

## مدیریت خطا

همه ابزارها با مدیریت قوی خطا طراحی شده‌اند:

-   خطاها به عنوان محتوای متنی برگردانده می‌شوند (هرگز پرتاب نمی‌شوند)، که ثبات پروتکل MCP را حفظ می‌کند
-   پیام‌های خطای توصیفی به تشخیص مشکلات کمک می‌کنند
-   وضعیت نشست حتی زمانی که عملیات‌های فردی شکست می‌خورند، حفظ می‌شود

## موارد استفاده

### تضمین کیفیت

-   اجرای موارد آزمون با هوش مصنوعی
-   آزمون رگرسیون بصری با اسکرین‌شات‌ها
-   حسابرسی دسترسی‌پذیری از طریق تحلیل درخت دسترسی‌پذیری

### استخراج وب و داده‌ها

-   پیمایش جریان‌های پیچیده چندصفحه‌ای
-   استخراج داده‌های ساختاریافته از محتوای پویا
-   مدیریت احراز هویت و نشست

### آزمون برنامه‌های موبایل

-   خودکارسازی آزمون چندسکویی (iOS + Android)
-   اعتبارسنجی جریان راه‌اندازی
-   آزمون پیوند عمیق و ناوبری

### آزمون یکپارچه‌سازی

-   آزمون جریان کار انتها به انتها
-   تأیید یکپارچه‌سازی API + رابط کاربری
-   بررسی سازگاری چندسکویی

## عیب‌یابی

### مرورگر شروع نمی‌شود

-   اطمینان حاصل کنید که Chrome نصب شده است
-   بررسی کنید که هیچ فرآیند دیگری از پورت اشکال‌زدایی پیش‌فرض (۹۲۲۲) استفاده نمی‌کند
-   اگر مشکلات نمایش رخ می‌دهد، حالت بدون سربرگ را امتحان کنید

### اتصال Appium ناموفق بود

-   تأیید کنید که سرور Appium در حال اجراست (`appium`)
-   URL و پیکربندی پورت Appium را بررسی کنید
-   اطمینان حاصل کنید که درایور مناسب نصب شده است (`appium driver list`)

### مشکلات شبیه‌ساز iOS

-   اطمینان حاصل کنید که Xcode نصب شده و به‌روز است
-   بررسی کنید که شبیه‌سازها در دسترس هستند (`xcrun simctl list devices`)
-   برای دستگاه‌های واقعی، تأیید کنید که UDID صحیح است

### مشکلات شبیه‌ساز Android

-   اطمینان حاصل کنید که Android SDK به درستی پیکربندی شده است
-   تأیید کنید که شبیه‌ساز در حال اجراست (`adb devices`)
-   بررسی کنید که متغیر محیطی `ANDROID_HOME` تنظیم شده است

## منابع

-   [مرجع ابزارها](./mcp/tools) - لیست کامل ابزارهای موجود
-   [راهنمای انتخابگرها](./mcp/selectors) - مستندات نحو انتخابگر
-   [پیکربندی](./mcp/configuration) - گزینه‌های پیکربندی
-   [سوالات متداول](./mcp/faq) - سوالات پرتکرار
-   [مخزن GitHub](https://github.com/webdriverio/mcp) - کد منبع و مشکلات
-   [بسته NPM](https://www.npmjs.com/package/@wdio/mcp) - بسته در npm
-   [پروتکل بافتار مدل](https://modelcontextprotocol.io/) - مشخصات MCP